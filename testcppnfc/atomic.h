#ifndef PROJECT_ATOMIC_COMPAT_H
#define PROJECT_ATOMIC_COMPAT_H

/*
 * LVGL FreeRTOS OSAL `atomic.h` bekliyor.
 * Arduino-ESP32 tarafinda dosya `freertos/atomic.h` olarak geliyor.
 */
#include <freertos/atomic.h>

#endif /* PROJECT_ATOMIC_COMPAT_H */
